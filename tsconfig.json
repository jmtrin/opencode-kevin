// root tsconfig — covers tests/scripts and delegates to workspaces via base
{
	"extends": "./tsconfig.base.json",
	"compilerOptions": {
		"outDir": "dist",
		"sourceMap": false
	},
	"include": ["plugin/**/*.ts", "scripts/**/*.ts", "tests/**/*.ts", "packages/*/src/**/*.ts"]
}
